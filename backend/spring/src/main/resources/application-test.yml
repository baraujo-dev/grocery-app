spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:grocery-db;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
