spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/grocery_db
    driver-class-name: org.postgresql.Driver
    username: grocery_user
    password: gu_PA$$!

auth:
  admin:
    username: admin@x.com
    password: admin
  jwt:
    expiration-ms: 300000
    secret: "dev-secret-change-me-please-32-bytes-long!"
